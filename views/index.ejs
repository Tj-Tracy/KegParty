<!doctype html>
<head>
    <link
</head>
<body>
   <% beerList.forEach((item) => { %>
    <p><%= item.name %></p>
    <p><%= item.description %></p>
    <a href="/<%= item.id %>"><%= item.id %></a>
   <% }) %>
    
</body>
